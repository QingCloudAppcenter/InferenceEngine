cat > /opt/app/conf/caddy/Caddyfile << CADDYFILE_EOF
{{ $myIp }}:80 {
  root /data/
  gzip
  browse /logs
  browse /models
  tls off

  upload / {
    yes_without_tls
    to "/data/models"
  }
}
CADDYFILE_EOF

